// lib/src/shared/constants/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xff2e6f40);
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Colors.white;
  static Color textMuted = Colors.black.withValues(alpha: .6);
  static Color backgroundGrey = Colors.grey[200]!;
}